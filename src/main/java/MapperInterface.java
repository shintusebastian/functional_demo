public interface MapperInterface {
    //int map(int x);
//    int map1(int a,int b);
    //void map(int x);
    boolean check(int x);//it takes some value, do some check on it and return true or false.
}
